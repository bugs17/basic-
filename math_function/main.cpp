#include <iostream>
#include <cmath> // kumpulan function math ada disini


int main(){


  double x = 3;
  double y = 4;
  double b = 5.49;
  double z;

  // z = std::max(x,y); // ini di gunakan untuk membandingkan nilai paling BESAR dari kedua nilai
  //  z = std::min(x,y); // ini di gunakan untuk membandingkan nilai paling KECIL dari kedua nilai

  // dari cmat
  // z = pow(x, y); // (pow) ini untuk operasi bilangan pangkat
  // z = sqrt(25); // (sqrt) akar quadrat
  // z = abs(-19); // (abs) absolute number atau nilai mutlak. ini kebalikan dari sebuah nilai minus
  // z = round(b); // rouund atau dasal. kalao .50 ke atas akan di bulatkan ke atas ke mawa maka bulatkan ke bawa
  // z = ceil(b); // ceil ini slalu pembulatan ke atas
  // z = floor(b); // kalo floor ini selalu pembulatan kebawah


  // untuk lebih lengkapnya soal math function bisa cek di www.cplusplus.com/reference/cmath ada banyak skali kaya sin,cos,tan



  std::cout << z;

  return 0;
}